# Multilingual Health QA — Dependencies
# Python 3.10 or 3.11 recommended
# Install: pip install -r requirements.txt

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.1.0                    # GPU build: pip install torch --index-url https://download.pytorch.org/whl/cu118
transformers==4.40.0            # pinned for PEFT compatibility
datasets>=2.18.0
peft>=0.10.0
accelerate>=0.28.0
bitsandbytes>=0.43.0            # Linux/WSL2 only; comment out on native Windows
sentencepiece>=0.2.0            # mT5 tokeniser

# ── Evaluation ───────────────────────────────────────────────────────────────
evaluate>=0.4.1
rouge_score>=0.1.2
sacrebleu>=2.4.0

# ── Data & Visualisation ─────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.13.0
scikit-learn>=1.3.0             # TF-IDF retrieval baseline + stratified split

# ── Config & Utilities ───────────────────────────────────────────────────────
pyyaml>=6.0.1                   # YAML config loading
tqdm>=4.66.0

# ── Jupyter / VS Code ────────────────────────────────────────────────────────
ipykernel>=6.29.0
ipywidgets>=8.1.0               # tqdm progress bars in notebooks
